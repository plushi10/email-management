// async function main() {
//     await testConnection();

//     type DBUser = userDate & RowDataPacket;
//     const [rows] = await pool.query<DBUser[]>("SELECT * FROM user"); 
//     console.log(rows);
//     const user = rows.map(row => new userUI(
//         row.email,
//         row.password,
//         row.username,
//         row.name,
//         row.surename,
//         row.dateBorn
//     ));
//     const email = user[0]?.email;
//     console.log(email);

// }

form user table :
    1 - for insert a new user

    INSERT INTO `user`  (email, password, username, name, surename, date)
    VALUES ('esempio@email.com', 'password123', 'username1', 'Mario', 'Rossi', '1990-05-20');

    2 - take the user by the email
    select u.email, u.password 
    from user u
    where u.email = "esempio@email.com"

